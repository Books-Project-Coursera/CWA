# Chạy từ thư mục code trên server. CHỈ CẦN MỘT LỆNH.
# Đổi run01 → run02/run03 cho lần chạy mới; tên đã tồn tại sẽ bị từ chối.

python main.py train --exp_name carparts_yolov8s_run01

# Lệnh trên tự TÁCH thành 2 EXPERIMENT nối nhau, vì swa đổi LR schedule còn
# top-k/ema thì không (xem train.run_experiments):
#
#   1. carparts_yolov8s_run01       methods=[top-k, ema]  cosine chuẩn   patience=10
#   2. carparts_yolov8s_run01_swa   methods=[swa]         constant LR     patience=0
#
# Mỗi experiment 5 seed [1,10,42,100,500] × 100 epoch ⇒ tổng 10 lần train.
# Cùng budget 100 epoch ⇒ công bằng giữa các method.
#
# Mọi tham số đã chốt trong config.py: SGD lr0=5e-3 cosine → lrf=0.01,
# EMA decay 0.996 (không warmup), SWA constant LR = (lr0+lr0·lrf)/2 = 2.525e-3
# từ 75% budget, K ∈ {2,3,4,5}.

# ---- Chạy lẻ từng phần nếu cần ----
# python main.py train --exp_name carparts_yolov8s_run01     --methods top-k ema
# python main.py train --exp_name carparts_yolov8s_swa_run01 --methods swa
